# Protect the public AI endpoint with an access code.
ACCESS_CODE=change-this-before-deploying

# Configure at least one model provider before first use.
OPENAI_API_KEY=
OPENAI_PROXY_URL=https://api.openai.com/v1
